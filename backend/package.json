{
  "name": "se-project-hub-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "npm install --prefix api",
    "build": "npm run build --prefix api",
    "lint": "npm run lint --prefix api",
    "test": "npm test --prefix api",
    "start": "npm run start --prefix api"
  },
  "devDependencies": {}
}
